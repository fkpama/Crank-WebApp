pool:
  name: Engie

variables:
    system.debug: true

steps:
- task: crankInstallerTask@0.0.6

- task: crankRunner@0.1.5
  inputs:
    persistBenchmarks: true
    configFile: 'api.benchmarks.yml'
    scenario: 'webapp'
    platform: 'build-agent'
    connectionString: 'Hello World'
