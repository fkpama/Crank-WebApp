pool:
  name: Engie


steps:
- task: crankRunnerTask@0.1.1
  inputs:
    configFile: 'api.benchmark.yml'
    scenario: 'webapp'
    platform: 'build-agent'
    connectionString: 'Hello World'
