pool:
  name: Engie

variables:
    system.debug: true

steps:
- task: crankRunnerTask@0.1.15
  inputs:
    persistBenchmarks: true
    configFile: 'api.benchmarks.yml'
    scenario: 'webapp'
    platform: 'build-agent'
    connectionString: 'Hello World'
